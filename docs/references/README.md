# references/  (image drop)

Reference / inspiration **images** for RIGRUNNER's look. Drop image files here (PNG/JPG,
`kebab-case` filenames), then add an entry in [`../references.md`](../references.md) saying what to
borrow from each. See that file for the convention. This directory holds only images; the index and
the "why" live in `references.md`.
